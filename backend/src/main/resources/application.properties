# ⚠️ PRODUCCIÓN: usar variables de entorno: DB_URL, DB_USERNAME, DB_PASSWORD
# Ej: spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/sistema_ventas?useSSL=false&serverTimezone=America/Guayaquil&allowPublicKeyRetrieval=true&characterEncoding=UTF-8}
//spring.datasource.url=jdbc:mysql://localhost:3306/sistema_ventas?useSSL=false&serverTimezone=America/Guayaquil&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
//spring.datasource.username=${DB_USERNAME:root}
//spring.datasource.password=${DB_PASSWORD:}
//spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



# ⚠️ PRODUCCIÓN: usar variables de entorno: DB_URL, DB_USERNAME, DB_PASSWORD
# Ej: spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/sistema_ventas?useSSL=false&serverTimezone=America/Guayaquil&allowPublicKeyRetrieval=true&characterEncoding=UTF-8}
spring.datasource.url=jdbc:mysql://193.203.175.197:3306/u862422702_SOA?useSSL=false&serverTimezone=America/Guayaquil&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
spring.datasource.username=u862422702_GRUPO3
spring.datasource.password=Ambato2026@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver




# Hibernate MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Inicialización SQL (Desactivada para persistir datos)
spring.sql.init.mode=never
spring.sql.init.continue-on-error=true
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# JWT
# ⚠️ PRODUCCIÓN: sobreescribir con variable de entorno JWT_SECRET (mínimo 64 bytes)
# Ej: app.jwt.secret=${JWT_SECRET:esta-es-una-clave-secreta-muy-larga-y-segura-para-el-sistema-de-ventas-2026-que-cumple-los-64-bytes-minimos}
app.jwt.secret=${JWT_SECRET:esta-es-una-clave-secreta-muy-larga-y-segura-para-el-sistema-de-ventas-2026-que-cumple-los-64-bytes-minimos}
app.jwt.expiration=86400000

# IVA
app.iva.porcentaje=15

# Empresa
app.empresa.nombre=Mi Empresa S.A.
app.empresa.ruc=1234567890001
app.empresa.direccion=Av. Principal 123

# CORS para Angular
spring.web.cors.allowed-origins=http://localhost:4200